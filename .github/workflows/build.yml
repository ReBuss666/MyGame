name: Build and Test

on:
  push:
    branches: [ main, master, feature/* ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Update system and install dependencies
      run: |
        pacman -Syu --noconfirm
        pacman -S --noconfirm base-devel gcc git sfml
    
    - name: Build project
      run: |
        mkdir -p build
        g++ -c -std=c++17 -O2 -Wall -Iinclude src/main.cpp -o build/main.o
        g++ -c -std=c++17 -O2 -Wall -Iinclude src/Rendering/FireEffect.cpp -o build/FireEffect.o
        g++ -c -std=c++17 -O2 -Wall -Iinclude src/Rendering/SectorRenderer.cpp -o build/SectorRenderer.o
        g++ -c -std=c++17 -O2 -Wall -Iinclude src/States/MenuState.cpp -o build/MenuState.o
        g++ -o MyGame build/main.o build/FireEffect.o build/SectorRenderer.o build/MenuState.o \
            -lsfml-graphics -lsfml-window -lsfml-audio -lsfml-network -lsfml-system
    
    - name: Run tests
      run: |
        # Добавьте здесь команды для запуска тестов
        echo "Tests would run here"
    
    - name: Upload Linux artifact
      uses: actions/upload-artifact@v4
      with:
        name: MyGame-Linux
        path: |
          MyGame
          assets/
          settings.cfg
        retention-days: 90

  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup MSVC
      uses: microsoft/setup-msbuild@v2
    
    - name: Cache SFML
      id: cache-sfml
      uses: actions/cache@v4
      with:
        path: C:\SFML
        key: sfml-3.0-windows-msvc-v2
    
    - name: Download and setup SFML 3
      if: steps.cache-sfml.outputs.cache-hit != 'true'
      shell: powershell
      run: |
        # Клонируем SFML 3 из GitHub (master ветка содержит SFML 3)
        git clone --depth 1 --branch master https://github.com/SFML/SFML.git C:\SFML-src
        
        # Собираем SFML
        mkdir C:\SFML-build
        cd C:\SFML-build
        cmake -G "Visual Studio 17 2022" -A x64 `
          -DCMAKE_INSTALL_PREFIX=C:\SFML `
          -DBUILD_SHARED_LIBS=TRUE `
          -DSFML_BUILD_EXAMPLES=FALSE `
          -DSFML_BUILD_DOC=FALSE `
          C:\SFML-src
        cmake --build . --config Release --parallel
        cmake --install . --config Release
    
    - name: Build project
      shell: cmd
      run: |
        call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
        
        mkdir build 2>nul || echo Build dir exists
        
        cl /c /EHsc /std:c++17 /O2 /Iinclude /IC:\SFML\include src\main.cpp /Fo:build\main.obj
        cl /c /EHsc /std:c++17 /O2 /Iinclude /IC:\SFML\include src\Rendering\FireEffect.cpp /Fo:build\FireEffect.obj
        cl /c /EHsc /std:c++17 /O2 /Iinclude /IC:\SFML\include src\Rendering\SectorRenderer.cpp /Fo:build\SectorRenderer.obj
        cl /c /EHsc /std:c++17 /O2 /Iinclude /IC:\SFML\include src\States\MenuState.cpp /Fo:build\MenuState.obj
        
        link /OUT:MyGame.exe ^
           build\main.obj ^
           build\FireEffect.obj ^
           build\SectorRenderer.obj ^
           build\MenuState.obj ^
           /LIBPATH:C:\SFML\lib ^
           sfml-graphics-3.lib sfml-window-3.lib sfml-audio-3.lib sfml-network-3.lib sfml-system-3.lib ^
           opengl32.lib winmm.lib gdi32.lib
    
    - name: Copy SFML DLLs
      shell: powershell
      run: |
        Copy-Item C:\SFML\bin\*.dll . -Force
    
    - name: Run tests
      run: |
        echo "Tests would run here"
    
    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: MyGame-Windows
        path: |
          MyGame.exe
          *.dll
          assets/
          settings.cfg
        retention-days: 90
